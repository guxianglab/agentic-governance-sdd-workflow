# Compatibility Note

This is the Claude Code edition. Claude Code primarily loads `CLAUDE.md` as project memory. This `AGENTS.md` is kept for other coding agents that understand AGENTS.md.

Follow the workflow and invariants in `CLAUDE.md` and `.sdd/`.
